/*
	AIM: To perform left and right shift
	Note: This program is running without the use of standard library
	Please use the following compiler options
	gcc -nostdlib main.c -o a.out
*/
char character_variable;	//Declared a char type variable but no memory
				//but no memory is allocated
				//scope of the variable is global
unsigned char mask;
char reversed;
int x;
void main(){
character_variable = 1;
mask = ~(mask);
mask = 0;

//for(x = 0;x < 10;x++)
//mask += 1;
x=0;
}
